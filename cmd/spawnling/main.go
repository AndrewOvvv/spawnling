package main

import (
	"fmt"
	"os"
)

func main() {
	// TODO: wire cobra root command
	// root := cli.NewRootCommand(socketPath)
	// if err := root.Execute(); err != nil {
	// 	os.Exit(1)
	// }
	fmt.Fprintln(os.Stderr, "spawnling: not implemented yet")
	os.Exit(1)
}
